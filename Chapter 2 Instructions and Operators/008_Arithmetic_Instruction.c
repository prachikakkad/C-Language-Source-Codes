#include <stdio.h>

int main()
{
    int d = ( 3*2 ) + 1; // Arithmetic Instruction because it contains arithmetic operators in it 
    printf("%d" , d);

    // We will learn Control Instruction in Upcoming Chapters

    return 0;
}