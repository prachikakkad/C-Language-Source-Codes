#include <stdio.h>

int main()
{
    printf("%lu\n", sizeof(char));
    printf("%lu\n", sizeof(int));
    printf("%lu\n", sizeof(float));
    printf("%lu", sizeof(double));

    // We will learn about remaining Miscellaneous Operators in upcoming Chapters.

    return 0;
}