#include <stdio.h>

int main()
{
    int a = 12;
    int b = 6;
    int c = 6;
    int d = 0;
    int p = 24 , r = 2;

    printf("a + b = %d\n", a + b); // Addition
    printf("a - b = %d\n", a - b); // Subtraction
    printf("a X b = %d\n", a * b); // Multiplication
    printf("a / b = %d\n", a / b); // Division 
    printf("p %% r = %d\n" , p % r); // Modulus

    return 0;
}