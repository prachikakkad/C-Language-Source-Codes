#include <stdio.h>

int main()
{
    int Prachi = 23;
    float R = 2.3;
    char D = 'R';

    printf("%d\n", Prachi); // ' %d ' is the format specifier
    printf("%f\n", R);      // ' %f ' is the format specifier
    printf("%c\n", D);      // ' %c ' is the format specifier

    return 0;
}